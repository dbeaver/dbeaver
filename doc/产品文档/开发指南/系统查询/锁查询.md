*此文档介绍在DBeaver中查询YashanDB的锁对象*

## 普通用户所需权限
普通用户查询锁需要有对v$session，v$lock，v$process，v$transaction，v$locked_object，dba_objects，DV$LOCK，DV$SESSION，DV$PROCESS的查询权限。

点击左侧锁菜单：  
![锁菜单](./image/锁查询/锁菜单.png)

弹出新界面，展示当前数据库下所有锁对象：
![锁对象](./image/锁查询/锁对象.png)
