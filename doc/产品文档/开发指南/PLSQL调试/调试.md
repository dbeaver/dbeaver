## 调试模式



while循环：

```sql
CREATE OR REPLACE PROCEDURE ya_proc2(b INT) IS
    a INT := 1;
BEGIN
    WHILE a<=b LOOP
        DBMS_OUTPUT.PUT_LINE ('while:' || a);
        a := a+1;
    END LOOP;
END;
```

![创建while示例](./image/创建while示例.png)



动态游标：