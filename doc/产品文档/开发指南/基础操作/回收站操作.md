## GRANT PERMISSIONS
回收站建议用系统用户打开，打开语句如下。
````sql
SELECT * FROM v$SYSTEM_PARAMETER WHERE NAME LIKE '%RECYCLEBIN%';
alter system set RECYCLEBIN_ENABLED = ON;
````

## VIEW RECYCLE BIN

在左侧点击模式，点击schema，点击回收站。

![查看回收站](./image/查看回收站.png)
